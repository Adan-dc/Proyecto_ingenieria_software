package com.donaciones.gestion.repository;

import com.donaciones.gestion.model.Talleres;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TalleresRepository extends JpaRepository<Talleres, Long> {
}
